# Traceability Map: Core 01

## 重複・横断関係

Fable 02 は core-03.1、05 は core-03.2、10/12 は core-03.4、09/12 は core-01 と重複する。

## 対応表

| Fable項目 | 問題 | 要求候補 | 仕様候補 | 実装項目候補 | テスト観点 | 状態 |
|---|---|---|---|---|---|---|
| 02 | setup/800-ollama-models.sh とスキルのモデル割当不整合。ワンライナー実行後に、Codex CLI、Ollamaサーバー、必要モデル、hooks、手動認証、verify が一貫した動作可能状態として保証されていない。setup が成功扱いでも、MAGI がフォールバック頼みになる、Codex 実装が使えない、自動エラー検知が無効になる可能性がある。 | ワンライナー完了後に最低限利用可能であるべき機能を定義する<br>自動導入できるものと手動認証が必要なものを分ける<br>fail / warn / info の境界を決める<br>Codex、Ollama、モデル、hooks、manual step の状態を一発で確認できるようにする | setup.sh --verify または setup/900-verify.sh のチェック項目<br>Codex CLI 自動インストールとログイン未完了時の扱い<br>Ollama未起動時の起動・常駐化・再実行案内<br>スキル要求モデルとpull対象モデルの一致条件<br>README上の手動ステップ導線 | setup/850-codex.sh は Codex CLI の確認のみで自動インストールしない<br>setup/401-ollama.sh はOllamaインストールまででサーバー起動しない<br>setup/800-ollama-models.sh は ollama list 失敗時に return 0 でモデル取得をスキップする<br>settings.json は hooks/error-detector.sh を参照するが hooks/ に実体がない<br>setup.sh に --verify / 900-verify はない | 素の新規環境でワンライナー後に verify が通るか<br>Ollama未起動、Codex未認証、モデル不足、error-detector欠落を検出できるか<br>手動ステップ未完了が warn / info として表示されるか<br>不要モデルをpullせず必要モデルをpullするか | 未確定 / 要整理 |
| 05 | error-detector.sh が配備されず自動エラー検知が無音で無効。ワンライナー実行後に、Codex CLI、Ollamaサーバー、必要モデル、hooks、手動認証、verify が一貫した動作可能状態として保証されていない。setup が成功扱いでも、MAGI がフォールバック頼みになる、Codex 実装が使えない、自動エラー検知が無効になる可能性がある。 | ワンライナー完了後に最低限利用可能であるべき機能を定義する<br>自動導入できるものと手動認証が必要なものを分ける<br>fail / warn / info の境界を決める<br>Codex、Ollama、モデル、hooks、manual step の状態を一発で確認できるようにする | setup.sh --verify または setup/900-verify.sh のチェック項目<br>Codex CLI 自動インストールとログイン未完了時の扱い<br>Ollama未起動時の起動・常駐化・再実行案内<br>スキル要求モデルとpull対象モデルの一致条件<br>README上の手動ステップ導線 | setup/850-codex.sh は Codex CLI の確認のみで自動インストールしない<br>setup/401-ollama.sh はOllamaインストールまででサーバー起動しない<br>setup/800-ollama-models.sh は ollama list 失敗時に return 0 でモデル取得をスキップする<br>settings.json は hooks/error-detector.sh を参照するが hooks/ に実体がない<br>setup.sh に --verify / 900-verify はない | 素の新規環境でワンライナー後に verify が通るか<br>Ollama未起動、Codex未認証、モデル不足、error-detector欠落を検出できるか<br>手動ステップ未完了が warn / info として表示されるか<br>不要モデルをpullせず必要モデルをpullするか | 未確定 / 要整理 |
| 08 | Codex CLI の自動インストール不足。ワンライナー実行後に、Codex CLI、Ollamaサーバー、必要モデル、hooks、手動認証、verify が一貫した動作可能状態として保証されていない。setup が成功扱いでも、MAGI がフォールバック頼みになる、Codex 実装が使えない、自動エラー検知が無効になる可能性がある。 | ワンライナー完了後に最低限利用可能であるべき機能を定義する<br>自動導入できるものと手動認証が必要なものを分ける<br>fail / warn / info の境界を決める<br>Codex、Ollama、モデル、hooks、manual step の状態を一発で確認できるようにする | setup.sh --verify または setup/900-verify.sh のチェック項目<br>Codex CLI 自動インストールとログイン未完了時の扱い<br>Ollama未起動時の起動・常駐化・再実行案内<br>スキル要求モデルとpull対象モデルの一致条件<br>README上の手動ステップ導線 | setup/850-codex.sh は Codex CLI の確認のみで自動インストールしない<br>setup/401-ollama.sh はOllamaインストールまででサーバー起動しない<br>setup/800-ollama-models.sh は ollama list 失敗時に return 0 でモデル取得をスキップする<br>settings.json は hooks/error-detector.sh を参照するが hooks/ に実体がない<br>setup.sh に --verify / 900-verify はない | 素の新規環境でワンライナー後に verify が通るか<br>Ollama未起動、Codex未認証、モデル不足、error-detector欠落を検出できるか<br>手動ステップ未完了が warn / info として表示されるか<br>不要モデルをpullせず必要モデルをpullするか | 未確定 / 要整理 |
| 09 | setup 内での Ollama サーバー起動・常駐化不足。ワンライナー実行後に、Codex CLI、Ollamaサーバー、必要モデル、hooks、手動認証、verify が一貫した動作可能状態として保証されていない。setup が成功扱いでも、MAGI がフォールバック頼みになる、Codex 実装が使えない、自動エラー検知が無効になる可能性がある。 | ワンライナー完了後に最低限利用可能であるべき機能を定義する<br>自動導入できるものと手動認証が必要なものを分ける<br>fail / warn / info の境界を決める<br>Codex、Ollama、モデル、hooks、manual step の状態を一発で確認できるようにする | setup.sh --verify または setup/900-verify.sh のチェック項目<br>Codex CLI 自動インストールとログイン未完了時の扱い<br>Ollama未起動時の起動・常駐化・再実行案内<br>スキル要求モデルとpull対象モデルの一致条件<br>README上の手動ステップ導線 | setup/850-codex.sh は Codex CLI の確認のみで自動インストールしない<br>setup/401-ollama.sh はOllamaインストールまででサーバー起動しない<br>setup/800-ollama-models.sh は ollama list 失敗時に return 0 でモデル取得をスキップする<br>settings.json は hooks/error-detector.sh を参照するが hooks/ に実体がない<br>setup.sh に --verify / 900-verify はない | 素の新規環境でワンライナー後に verify が通るか<br>Ollama未起動、Codex未認証、モデル不足、error-detector欠落を検出できるか<br>手動ステップ未完了が warn / info として表示されるか<br>不要モデルをpullせず必要モデルをpullするか | 未確定 / 要整理 |
| 10 | セットアップ統合検証 doctor / verify の不足。ワンライナー実行後に、Codex CLI、Ollamaサーバー、必要モデル、hooks、手動認証、verify が一貫した動作可能状態として保証されていない。setup が成功扱いでも、MAGI がフォールバック頼みになる、Codex 実装が使えない、自動エラー検知が無効になる可能性がある。 | ワンライナー完了後に最低限利用可能であるべき機能を定義する<br>自動導入できるものと手動認証が必要なものを分ける<br>fail / warn / info の境界を決める<br>Codex、Ollama、モデル、hooks、manual step の状態を一発で確認できるようにする | setup.sh --verify または setup/900-verify.sh のチェック項目<br>Codex CLI 自動インストールとログイン未完了時の扱い<br>Ollama未起動時の起動・常駐化・再実行案内<br>スキル要求モデルとpull対象モデルの一致条件<br>README上の手動ステップ導線 | setup/850-codex.sh は Codex CLI の確認のみで自動インストールしない<br>setup/401-ollama.sh はOllamaインストールまででサーバー起動しない<br>setup/800-ollama-models.sh は ollama list 失敗時に return 0 でモデル取得をスキップする<br>settings.json は hooks/error-detector.sh を参照するが hooks/ に実体がない<br>setup.sh に --verify / 900-verify はない | 素の新規環境でワンライナー後に verify が通るか<br>Ollama未起動、Codex未認証、モデル不足、error-detector欠落を検出できるか<br>手動ステップ未完了が warn / info として表示されるか<br>不要モデルをpullせず必要モデルをpullするか | 未確定 / 要整理 |
| 12 | セットアップ後の手動ステップ・チェックリスト不足。ワンライナー実行後に、Codex CLI、Ollamaサーバー、必要モデル、hooks、手動認証、verify が一貫した動作可能状態として保証されていない。setup が成功扱いでも、MAGI がフォールバック頼みになる、Codex 実装が使えない、自動エラー検知が無効になる可能性がある。 | ワンライナー完了後に最低限利用可能であるべき機能を定義する<br>自動導入できるものと手動認証が必要なものを分ける<br>fail / warn / info の境界を決める<br>Codex、Ollama、モデル、hooks、manual step の状態を一発で確認できるようにする | setup.sh --verify または setup/900-verify.sh のチェック項目<br>Codex CLI 自動インストールとログイン未完了時の扱い<br>Ollama未起動時の起動・常駐化・再実行案内<br>スキル要求モデルとpull対象モデルの一致条件<br>README上の手動ステップ導線 | setup/850-codex.sh は Codex CLI の確認のみで自動インストールしない<br>setup/401-ollama.sh はOllamaインストールまででサーバー起動しない<br>setup/800-ollama-models.sh は ollama list 失敗時に return 0 でモデル取得をスキップする<br>settings.json は hooks/error-detector.sh を参照するが hooks/ に実体がない<br>setup.sh に --verify / 900-verify はない | 素の新規環境でワンライナー後に verify が通るか<br>Ollama未起動、Codex未認証、モデル不足、error-detector欠落を検出できるか<br>手動ステップ未完了が warn / info として表示されるか<br>不要モデルをpullせず必要モデルをpullするか | 未確定 / 要整理 |

## 注意

状態はすべて暫定。要求・仕様・実装計画・テスト設計の各段階で更新する。